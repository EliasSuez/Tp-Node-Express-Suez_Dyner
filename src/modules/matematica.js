const PI = 3.14;
const numeros = ["dos", "cuatro", "ocho", "diez"]
function sumar(x, y) {
    let total
 return total = x + y
}

const multiplicar = (a, b) => {
    let total
 return total = a * b
}; 

const dividir = (c, d) => {
    let total
    return total = c / d;
}

const restar = (e, f) => { 

    let total
    return total = e - f;
}

export {PI, sumar, multiplicar, dividir, restar, numeros};